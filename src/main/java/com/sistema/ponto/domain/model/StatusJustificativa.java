package com.sistema.ponto.domain.model;

/**
 * Enum de domínio que define os possíveis status de uma justificativa.
 * Livre de qualquer anotação de framework.
 * <p>
 * Pacote: domain.model
 */
public enum StatusJustificativa {
    PENDENTE,
    APROVADA,
    REJEITADA
}
